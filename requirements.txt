# ==================== 核心依赖 ====================
playwright==1.40.0          # 浏览器自动化工具
pytest==7.4.3               # 测试框架
allure-pytest==2.13.2       # Allure测试报告插件

# ==================== 辅助依赖 ====================
openpyxl==3.1.2             # Excel文件处理（备用）
python-dotenv==1.0.0        # 环境变量管理（可选）
PyYAML==6.0.1               # YAML配置文件解析（可选）

sshtunnel>=0.4.0